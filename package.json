// package.json
{
  "name": "key-validator-function",
  "version": "1.0.0",
  "description": "Appwrite function to validate a license key and machine ID.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}
